# OpenUI5 Theme Designer - Environment Variables Configuration

# =============================================================================
# OAuth / Keycloak Configuration
# =============================================================================

# Keycloak Server URL (e.g., https://keycloak.example.com)
KEYCLOAK_URL=https://your-keycloak-server.com

# Keycloak Realm Name
KEYCLOAK_REALM=your-realm

# OAuth Client ID (registered in Keycloak)
CLIENT_ID=theme-designer

# OAuth Client Secret (from Keycloak client configuration)
CLIENT_SECRET=your-client-secret-here

# Session Secret (generate with: openssl rand -hex 32)
SESSION_SECRET=your-session-secret-here

# =============================================================================
# Theme Builder Configuration
# =============================================================================

# Theme Builder instances self-register with the Designer via socket.io (see
# DESIGNER_URL / SELF_URL on each theme-builder-api service in docker-compose.yml)
# — no static URL map needed on the Designer side anymore.

# Preferred default UI5 Version (used when a theme has no version specified, and
# when preselecting a version in the UI). If this version isn't currently
# registered by any builder, the first connected builder is used instead.
DEFAULT_UI5_VERSION=1.96.40

# =============================================================================
# Database Configuration
# =============================================================================

# SQLite Database Path (relative to theme-designer-app/server)
DATABASE_PATH=/app/server/data/themes.db

# =============================================================================
# Application Configuration
# =============================================================================

# Node Environment (development | production)
NODE_ENV=production

# =============================================================================
# Notes
# =============================================================================
#
# To add a new UI5 version:
# 1. Add a new theme-builder-api service to docker-compose.yml (e.g., theme-builder-1-130)
#    with UI5_VERSION, DESIGNER_URL and its own SELF_URL set
# 2. That's it — it self-registers with the Designer and shows up in the UI
#    automatically, no changes needed on the Designer side
#
# Session Secret Generation:
#   openssl rand -hex 32
#
# OAuth Client Setup (Keycloak):
# 1. Create new client in Keycloak admin console
# 2. Set Access Type to "confidential"
# 3. Add Valid Redirect URIs: https://your-domain.com/auth/callback
# 4. Copy Client Secret from Credentials tab
#
